[package]
name = "garden-hal"
description = "Hardware abstraction for the Garden edge agent. Traits plus a clock, so the brain can run against a simulator."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
garden-core.workspace = true
jiff.workspace = true
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
